---
image: /generated/articles-docs-prereleases.png
id: prereleases
slug: /prereleases
title: 测试预发布版本
crumb: "只有勇者"
---

import {Prerelease} from "../components/PrereleaseVersion"

<Prerelease />

:::warning
确保从版本中删除 `^` 字符。如果不这样做，您将获得哈希值字母顺序最高的版本，这是 Remotion 的一个随机版本，而不是您想要的版本。
:::

之后，分别运行 `yarn`、`npm i` 或 `pnpm i`。
