---
image: /generated/articles-docs-noise-noise-4d.png
title: noise4D()
crumb: "生成一些"
---

_来自 [`@remotion/noise`](/docs/noise) 包._

创建 4D 噪声。

## API

该函数接受五个参数：

### `seed`

传入任何 _字符串_ 或 _数字_。如果种子相同，则对于相同的 `x`、`y`、`z` 和 `w` 值将获得相同的结果。更改种子以获得不同的结果，适用于您的 `x`、`y`、`z`、`w` 值。

### `x`

_数字_

第一个维度值。

### `y`

_数字_

第二个维度值。

### `z`

_数字_

第三个维度值。

### `w`

_数字_

第四个维度值。

## 返回值

一个值在 `-1` 和 `1` 之间，随着您的 `x`、`y`、`z` 和 `w` 值的变化而摆动。

## 示例

```tsx twoslash
import { noise4D } from "@remotion/noise";

const x = 32;
const y = 40;
const z = 50;
const w = 64;
console.log(noise4D("my-seed", x, y, z, w));
```

## Credits

依赖项: [simplex-noise](https://www.npmjs.com/package/simplex-noise)

## 另请参阅

- [示例: 噪声可视化](/docs/noise-visualization)
- [noise2D()](/docs/noise/noise-2d)
- [noise3D()](/docs/noise/noise-3d)
- [此函数的源代码](https://github.com/remotion-dev/remotion/blob/main/packages/noise/src/index.ts)
- [`@remotion/noise`](/docs/noise)

