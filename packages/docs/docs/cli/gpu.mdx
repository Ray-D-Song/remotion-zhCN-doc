---
image: /generated/articles-docs-cli-gpu.png
crumb: CLI 参考
sidebar_label: gpu
title: npx remotion gpu
---

_从 Remotion v4.0.52 开始可用_

显示 Chrome 浏览器如何使用 GPU。

```bash
npx remotion gpu --gl=angle
```

该命令接受与 `npx remotion render` 相同的 `--gl` 参数，并且还会使用 [`Config.setChromiumOpenGlRenderer()`](/docs/config#setchromiumopenglrenderer) 选项。  
尝试不同的值，找出哪个对您的系统最佳。

```bash title="示例输出"
Canvas: Hardware accelerated
Canvas out-of-process rasterization: Enabled
Direct Rendering Display Compositor: Disabled
Compositing: Hardware accelerated
Multiple Raster Threads: Enabled
OpenGL: Enabled
Rasterization: Hardware accelerated
Raw Draw: Disabled
Skia Graphite: Disabled
Video Decode: Hardware accelerated
Video Encode: Hardware accelerated
WebGL: Hardware accelerated
WebGL2: Hardware accelerated
WebGPU: Hardware accelerated
```

输出不应用于自动化解析，因为它可能在任何 Remotion 和 Chrome 版本之间更改。

## 另请参阅

- [此功能的源代码](https://github.com/remotion-dev/remotion/blob/main/packages/cli/src/gpu.ts)
- [使用 GPU](/docs/gpu)

