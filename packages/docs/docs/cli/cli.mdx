---
image: /generated/articles-docs-cli-cli.png
title: 命令行参考
sidebar_label: CLI 参考
id: cli
---

import {TableOfContents} from '../../components/TableOfContents/cli';

## 如何使用

您可以通过安装 `@remotion/cli` 并运行以下命令来运行 CLI：

- 在 npm 项目中运行 `npx remotion`
- 在 Yarn 项目中运行 `yarn remotion`
- 在 pnpm 项目中运行 `pnpm exec remotion`
- 在 Bun 项目中运行 `bunx remotion`

为简洁起见，在文档中我们总是使用 `npx remotion`。

在 npm 脚本中，您不需要 `npx` 前缀：

```json title="package.json"
{
  "scripts": {
    "render": "remotion render"
  }
}
```

### 使用 Bun<AvailableFrom v="4.0.118" />

默认情况下，`npx remotion` 命令是使用 Node 执行的。  
即使 `bunx remotion` 也是使用 Node，除非您添加 `--bun` 标志。  
要使用 Bun，请将 `remotion` 替换为 `remotionb`。

```json title="package.json"
{
  "scripts": {
    "render": "remotionb render"
  }
}
```

## 命令

以下命令可用 - 您可以始终使用 `npx remotion` 运行它们，甚至如果将命令放在 npm 脚本中，则无需 `npx` 前缀。

<TableOfContents />

## 示例命令

```
npx remotion render --codec=vp8 HelloWorld out/video.webm
```

## Fig.io 自动补全

安装 [Fig](https://fig.io)（仅限 macOS）以将 Remotion 自动补全添加到您的终端。  
键入 `npx remotion` 开始获取建议。

## 参见

- [渲染您的视频](/docs/render)
- [配置文件](/docs/config)

