---
image: /generated/articles-docs-cli-bundle.png
title: npx remotion bundle
sidebar_label: bundle
crumb: CLI 参考
---

_从 v4.0.89 版本开始可用_

在命令行上创建一个[Remotion Bundle](/docs/terminology/bundle)。  
相当于[`bundle()`](/docs/bundle) Node.JS API。

```bash
npx remotion bundle <serve-url|entry-file>?
```

您可以将[Serve URL](/docs/terminology/serve-url)或[入口点](/docs/terminology/entry-point)作为第一个参数传递，否则入口点将会被[确定](/docs/terminology/entry-point#which-entry-point-is-being-used)。

## 标志

### `--config`

指定 Remotion 配置文件的位置。

### `--log`

<Options id="log" />

### `--public-dir`

<Options id="public-dir" />

### `--out-dir`

定义生成的 bundle 的位置。默认情况下，它是一个名为 `./build` 的文件夹，与[Remotion Root](/docs/terminology/remotion-root)相邻。

### `--public-path`<AvailableFrom v="4.0.127"/>

<Options id="public-path" />

### `--disable-git-source`<AvailableFrom v="4.0.182" />

<Options id="disable-git-source" />
