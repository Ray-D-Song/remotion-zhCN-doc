---
image: /generated/articles-docs-paths-get-length.png
title: getLength()
crumb: "@remotion/paths"
---

_属于 [`@remotion/paths`](/docs/paths) 包。_

获取 SVG 路径的长度。参数必须是有效的 SVG 路径属性。

如果路径有效，则返回一个数字：

```tsx twoslash
import { getLength } from "@remotion/paths";

const length = getLength("M 0 0 L 100 0");
console.log(length); // 100
```

如果路径无效，该函数将抛出错误：

```tsx twoslash
import { getLength } from "@remotion/paths";
// ---cut---
getLength("remotion"); // Error: Malformed path data: ...
```

## Credits

源代码主要来自 [svg-path-properties](https://www.npmjs.com/package/svg-path-properties)。

## 另请参阅

- [getPointAtLength()](/docs/paths/get-point-at-length)
- [getTangentAtLength()](/docs/paths/get-point-at-length)
- [此函数的源代码](https://github.com/remotion-dev/remotion/blob/main/packages/paths/src/get-length.ts)
- [`@remotion/paths`](/docs/paths)

