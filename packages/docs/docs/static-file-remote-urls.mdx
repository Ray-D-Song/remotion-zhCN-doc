---
image: /generated/articles-docs-static-file-remote-urls.png
id: staticfile-remote-urls
title: staticFile() 不支持远程 URL
sidebar_label: staticFile() 远程 URL
crumb: "故障排除"
---

如果您收到以下错误消息：

```
staticFile() 不支持远程 URL。相反，请直接传递 URL，而无需将其包装在 staticFile() 中。
```

您尝试将远程 URL 传递给 `staticFile()`。您不需要在 `staticFile` 中包装路径，可以直接传递：

```tsx twoslash title="❌ staticFile() 内部的远程 URL"
import { Img, staticFile } from "remotion";

const MyComp = () => {
  return <Img src={staticFile("https://example.com/image.png")} />;
};
```

而是：

```tsx twoslash title="✅ 直接传递远程 URL"
import { Img } from "remotion";

const MyComp = () => {
  return <Img src={"https://example.com/image.png"} />;
};
```

## 另请参阅

- [`staticFile()`](/docs/staticfile)

