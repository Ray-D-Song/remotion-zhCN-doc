---
image: /generated/articles-docs-troubleshooting-nextjs-image.png
sidebar_label: 使用 Next.js 图像时闪烁
title: 使用 Next.js <Image> 标签时出现闪烁
crumb: "常见错误"
---

以下代码在 Remotion 中不推荐使用：

```tsx title="❌ 不要这样做"
import Image from "next/image";

const myMarkup = <Image src="https://picsum.photos/200/300"></Image>;
```

## 问题

Remotion 无法知道图像何时加载完成，因为通过 `<Image>` 加载图像时无法确定。

这将导致 Remotion 在渲染过程中不等待图像加载完成，从而导致可见的闪烁。

## 解决方案

改用 [`<Img>`](/docs/img) 标签：

```tsx twoslash title="✅ 这样做"
const src = "abc";
// ---cut---
import { AbsoluteFill, Img } from "remotion";

const myMarkup = <Img src="https://picsum.photos/200/300" />;
```

## 参见

- [`<Img>`](/docs/img)
- [闪烁](/docs/flickering)
- [使用背景图像时闪烁](/docs/troubleshooting/background-image)

