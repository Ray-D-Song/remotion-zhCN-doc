---
image: /generated/articles-docs-media-parser-node-reader.png
id: node-reader
title: nodeReader
slug: /media-parser/node-reader
crumb: "@remotion/media-parser"
---

:::warning
**不稳定的 API**：此包是实验性的。我们可能随时更改 API，直到删除此通知。
:::


一个用于 `@remotion/media-parser` 的读取器，使用 Node.js `fs` 模块从本地文件系统读取。  
它也与 Bun 一起工作。  

## 示例

```tsx twoslash title="读取本地文件"
// @module: es2022
// @target: es2017
import {parseMedia} from '@remotion/media-parser';
import {nodeReader} from '@remotion/media-parser/node';

const result = await parseMedia({
  src: '/Users/jonnyburger/Downloads/my-video.mp4',
  fields: {
    durationInSeconds: true,
    dimensions: true,
  },
  reader: nodeReader,
});
```
