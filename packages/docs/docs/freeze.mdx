---
image: /generated/articles-docs-freeze.png
id: freeze
title: <冻结>
crumb: "API"
---

import { FreezeExample } from "../components/FreezeExample/FreezeExample";

```twoslash include example
const BlueSquare: React.FC = () => <div></div>
// - BlueSquare
```

_从 v2.2.0 版本开始可用。_

当使用 `<冻结/>` 组件时，所有子元素都将冻结在您指定的帧上。

如果一个组件是 `<冻结/>` 的子元素，调用 [`useCurrentFrame()`](/docs/use-current-frame) 钩子将始终返回您指定的帧编号，而不受任何 [`<序列>`](/docs/sequence) 的影响。

[`<视频 />`](/docs/video) 和 [`<离线视频 />`](/docs/video) 元素将被暂停，而 [`<音频 />`](/docs/audio) 元素将以静音方式渲染。

## 示例

```tsx twoslash title="MyComp.tsx"
// @include: example-BlueSquare
// ---cut---
import { Freeze } from "remotion";

const MyVideo = () => {
  return (
    <Freeze frame={30}>
      <BlueSquare />
    </Freeze>
  );
};
```

## API

冻结组件是一个高阶组件，除了它的子元素外，还接受以下 props：

### `frame`

子元素应该在哪一帧冻结。

### `active`<AvailableFrom v="4.0.127"/>

通过传递 `false` 来停用冻结组件。  
您还可以传递一个回调函数，并接受当前帧并返回一个布尔值。

```tsx twoslash title="从第30帧开始"
// @include: example-BlueSquare
// ---cut---
import { Freeze } from "remotion";

const MyVideo = () => {
  return (
    <Freeze frame={30} active={(f) => f < 30}>
      <BlueSquare />
    </Freeze>
  );
};
```

## 演示

<FreezeExample />

## 另请参阅

- [此组件的源代码](https://github.com/remotion-dev/remotion/blob/main/packages/core/src/freeze.tsx)
- [`<视频/>` 播放速度](/docs/video#playbackrate)
- [`useCurrentFrame()`](/docs/use-current-frame)

