---
image: /generated/articles-docs-static-file-relative-paths.png
id: staticfile-relative-paths
title: staticFile() 不支持相对路径
sidebar_label: staticFile() 相对路径
crumb: "故障排除"
---

如果您收到以下错误消息：

```
staticFile() 不支持相对路径。请传递位于 public/ 文件夹内的文件名。
```

您可能尝试传递相对路径给 `staticFile()`：

```tsx twoslash title="❌ 相对路径"
import { staticFile } from "remotion";
staticFile("../public/image.png");
```

```tsx twoslash title="❌ 文件不应该有 ./ 前缀"
import { staticFile } from "remotion";
staticFile("./image.png");
```

或者您可能尝试传递绝对路径：

```tsx twoslash title="❌ 文件不应该使用绝对路径"
import { staticFile } from "remotion";
staticFile("/Users/bob/remotion-project/public/image.png");
```

或者您可能尝试添加一个不必要的 public/ 前缀：

```tsx twoslash title="❌ 文件不应该包含 public/ 前缀"
import { staticFile } from "remotion";
staticFile("public/image.png");
```

相反，直接传递位于 public 文件夹内的文件名：

```tsx twoslash title="✅ 文件名"
import { staticFile } from "remotion";
staticFile("image.png");
```

## 另请参阅

- [`staticFile()`](/docs/staticfile)
