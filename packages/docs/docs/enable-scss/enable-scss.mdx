---
image: /generated/articles-docs-enable-scss-enable-scss.png
title: enableScss()
slug: /enable-scss/enable-scss
crumb: "@remotion/enable-scss"
---

_从v4.0.162版本开始可用_

一个函数，修改默认的Webpack配置以进行必要的更改，以支持SASS/SCSS。

```ts twoslash title="remotion.config.ts"
import { Config } from "@remotion/cli/config";
import { enableScss } from "@remotion/enable-scss";

Config.overrideWebpackConfig((currentConfiguration) => {
  return enableScss(currentConfiguration);
});
```

如果您想进行其他配置更改，可以通过使用reducer风格来实现：

```ts twoslash title="remotion.config.ts"
import { Config } from "@remotion/cli/config";
import { enableScss } from "@remotion/enable-scss";

Config.overrideWebpackConfig((currentConfiguration) => {
  return enableScss({
    ...currentConfiguration,
    // Make other changes
  });
});
```

## 另请参阅

- [此函数的源代码](https://github.com/remotion-dev/remotion/blob/main/packages/enable-scss/src/enable-scss.ts)

